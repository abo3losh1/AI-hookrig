# broken-hooks

A settings file with the mistakes this tool exists to find. Every hook here
looks fine on macOS and does nothing, or the wrong thing, on Windows.

```bash
npx hookrig doctor --dir examples/broken-hooks
```
